__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.DS_Store

# Local state — never commit credentials
state/
token.json
*.token
# Atomic-write temp files briefly hold a live token next to the real one
*.tmp
*.lock

# Build artifacts
dist/
build/
*.whl
